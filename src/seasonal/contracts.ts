export interface ExpansionManifest{expansionId:string;developerId:string;version:string;apiVersion:string;minimumGameVersion:string;permissions:('read_profile'|'read_appearance'|'submit_score'|'request_reward')[];startsAt:string;endsAt:string;allowedEndpoints:string[];checksumSha256:string;signature:string;approvalStatus:'pending'|'approved'|'suspended'|'revoked'}
export interface SeasonalResult{sessionId:string;submissionId:string;playerId:string;expansionId:string;score:number;objectives:string[];completedAt:string}
export interface ISeasonalService{startSession(playerId:string,expansionId:string):Promise<{sessionId:string;expiresAt:string}>;submitResult(result:SeasonalResult):Promise<{accepted:boolean;rewardClaimId?:string}>;claimReward(playerId:string,rewardClaimId:string,idempotencyKey:string):Promise<{awarded:boolean}>}
