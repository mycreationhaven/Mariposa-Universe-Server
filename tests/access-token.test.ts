import{describe,expect,it}from'vitest';import{AccessTokenService}from'../src/auth/access-token.js';describe('access tokens',()=>{const s=new AccessTokenService('x'.repeat(32),60);it('preserves signed identity',()=>{const t=s.issue({userId:'u1',characterId:'c1',displayName:'Player'},100);expect(s.verify(t.accessToken,101).characterId).toBe('c1');});it('rejects tampering',()=>{const t=s.issue({userId:'u1',characterId:'c1',displayName:'Player'},100);expect(()=>s.verify(t.accessToken+'x',101)).toThrow();});it('rejects expiry',()=>{const t=s.issue({userId:'u1',characterId:'c1',displayName:'Player'},100);expect(()=>s.verify(t.accessToken,161)).toThrow('Expired');});});
