import { describe,expect,it } from 'vitest';import { simulate,spawnBody } from '../src/movement/simulation.js';import { MOTION,WORLD } from '../src/movement/constants.js';
const input=(sequence:number,left=false,right=false,jump=false)=>({sequence,left,right,jump,clientTime:sequence});
describe('authoritative movement',()=>{it('accelerates but never exceeds max speed',()=>{const b=spawnBody();for(let i=1;i<300;i++)simulate(b,input(i,false,true),1/60);expect(b.vx).toBe(MOTION.maxRunSpeed);expect(b.x).toBeLessThanOrEqual(WORLD.width-WORLD.playerWidth);});it('jumps only from floor/coyote window',()=>{const b=spawnBody();simulate(b,input(1,false,false,true),1/60);const first=b.vy;simulate(b,input(2,false,false,true),1/60);expect(first).toBeLessThan(0);expect(b.vy).toBeGreaterThan(first);});it('falls and lands exactly on floor',()=>{const b=spawnBody();simulate(b,input(1,false,false,true),1/60);for(let i=2;i<200;i++)simulate(b,input(i),1/60);expect(b.y).toBe(WORLD.floorY-WORLD.playerHeight);expect(b.grounded).toBe(true);expect(b.vy).toBe(0);});it('clamps maliciously large delta time',()=>{const b=spawnBody();simulate(b,input(1,false,true),100);expect(b.x).toBeLessThan(300);});});
